     1→#!/usr/bin/env python3"""================================================================================SMART DCA BACKTEST SUITE — Full Quantitative Research Script================================================================================Tests 5 DCA strategies against 3-year and 5-year BTC historical periods:  1. Standard DCA (Benchmark)  2. Style C  (On-Chain Tiered Pure DCA)  3. Style E  (Smart Rebalance & Top Skimming)  4. Style G v2 (Adaptive Hybrid Flagship)  5. Style Alpha (Innovated — This Script)Author : AI Quantitative AnalystData   : CoinGecko (Price) + BGeometrics (MVRV, NUPL, SOPR) / Realistic MockOutput : Summary tables + Matplotlib charts saved to /home/z/my-project/download/================================================================================"""# ============================================================# SECTION 0: IMPORTS & GLOBAL CONSTANTS# ============================================================import osimport sysimport timeimport jsonimport warningsimport numpy as npimport pandas as pdimport matplotlibimport matplotlib.pyplot as pltimport matplotlib.ticker as mtickerimport requestsfrom datetime import datetime, timedelta# --------------------------------------------------------------# Font configuration for Thai Baht symbol and CJK characters# --------------------------------------------------------------matplotlib.font_manager.fontManager.addfont(    '/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf')plt.rcParams['font.sans-serif'] = ['DejaVu Sans']plt.rcParams['axes.unicode_minus'] = Falsewarnings.filterwarnings('ignore')# ============================================================# GLOBAL CONSTANTS# ============================================================BASE_BUDGET_THB   = 100       # Fixed daily budget in THBUSD_THB_RATE      = 36        # Fixed exchange rate: 1 USD = 36 THBBUY_FEE_PCT      = 0.0015    # 0.15% total execution friction (0.1% fee + 0.05% slip)SELL_FEE_PCT      = 0.0015    # Same friction on sellsDOWNLOAD_DIR      = '/home/z/my-project/download'os.makedirs(DOWNLOAD_DIR, exist_ok=True)print("=" * 70)print("  SMART DCA BACKTEST SUITE")print("  Strategies: Standard | Style C | Style E | Style G v2 | Style Alpha")print("=" * 70)# ============================================================# SECTION 1: DATA PIPELINE — FETCH & MOCK# ============================================================def fetch_coingecko_btc_price(days=1826):    """    Attempt to fetch BTC daily close prices from CoinGecko's free API.    Returns a DataFrame with columns ['date', 'price_usd'].    If the API fails (rate limit, network error), returns None so    we fall back to mock data.    """    url = (        f"https://api.coingecko.com/api/v3/coins/bitcoin/market_chart"        f"?vs_currency=usd&days={days}&interval=daily"    )    headers = {'accept': 'application/json'}    try:        print("[DATA] Fetching BTC prices from CoinGecko...")        resp = requests.get(url, headers=headers, timeout=30)        if resp.status_code == 429:            print("[DATA] CoinGecko rate-limited. Will use mock data.")            return None        resp.raise_for_status()        data = resp.json()        prices = data.get('prices', [])        if not prices:            return None        records = []        for ts, price in prices:            dt = datetime.utcfromtimestamp(ts / 1000).date()            records.append({'date': dt, 'price_usd': price})        df = pd.DataFrame(records).drop_duplicates(subset='date').sort_values('date').reset_index(drop=True)        print(f"[DATA] CoinGecko returned {len(df)} days of price data.")        return df    except Exception as e:        print(f"[DATA] CoinGecko fetch failed: {e}. Will use mock data.")        return Nonedef fetch_bgeometrics_metric(metric_name, token='7NqNRwWhyc'):    """    Fetch a single on-chain metric (MVRV, NUPL, SOPR) from BGeometrics API.    Returns a DataFrame with columns ['date', metric_name], or None on failure.    """    url = f"https://api.bgeometrics.com/v1/{metric_name}?token={token}"    try:        resp = requests.get(url, timeout=15)        if resp.status_code != 200:            return None        data = resp.json()        # The API response format may vary; try common structures        records = []        if isinstance(data, list):            for item in data:                date_val = item.get('date') or item.get('timestamp') or item.get('t')                metric_val = item.get('value') or item.get(metric_name) or item.get('v')                if date_val and metric_val is not None:                    if isinstance(date_val, (int, float)):                        dt = datetime.utcfromtimestamp(date_val / 1000).date()                    else:                        dt = pd.to_datetime(date_val).date()                    records.append({'date': dt, metric_name: float(metric_val)})        elif isinstance(data, dict):            series = data.get('data') or data.get('values') or data.get('results')            if isinstance(series, list):                for item in series:                    date_val = item.get('date') or item.get('timestamp')                    metric_val = item.get('value') or item.get(metric_name)                    if date_val and metric_val is not None:                        if isinstance(date_val, (int, float)):                            dt = datetime.utcfromtimestamp(date_val / 1000).date()                        else:                            dt = pd.to_datetime(date_val).date()                        records.append({'date': dt, metric_name: float(metric_val)})        if not records:            return None        df = pd.DataFrame(records).drop_duplicates(subset='date').sort_values('date').reset_index(drop=True)        return df    except Exception as e:        print(f"[DATA] BGeometrics {metric_name} fetch failed: {e}")        return Nonedef generate_mock_btc_prices(start_date, end_date, seed=42):    """    Generate realistic mock BTC daily prices using a geometric Brownian motion    with mean-reverting drift calibrated to historical BTC behavior.    This ensures the backtest code is fully functional and executable    even without API access.    """    np.random.seed(seed)    dates = pd.date_range(start=start_date, end=end_date, freq='D')    n = len(dates)    # Starting price ~$27,000 (mid-2023 level)    prices = np.zeros(n)    prices[0] = 27000.0    # GBM parameters calibrated to BTC:    #   mu  = annual drift ~15%  (long-term BTC appreciation)    #   sigma = annual vol ~65% (typical BTC realized vol)    mu = 0.15    sigma = 0.65    dt = 1 / 365.25    for i in range(1, n):        # Add mean-reversion pull toward a log-linear trend        trend = prices[0] * np.exp(mu * i * dt)        pull_strength = 0.002  # gentle pull        pull = pull_strength * (np.log(trend) - np.log(max(prices[i-1], 1)))        z = np.random.standard_normal()        prices[i] = prices[i-1] * np.exp((mu - 0.5 * sigma**2) * dt + pull + sigma * np.sqrt(dt) * z)        # Clamp to realistic range    prices = np.clip(prices, 3000, 250000)    df = pd.DataFrame({'date': dates.date, 'price_usd': prices})    print(f"[DATA] Generated {n} days of mock BTC prices ({start_date} to {end_date}).")    print(f"        Price range: ${prices.min():,.0f} - ${prices.max():,.0f}")    return dfdef generate_mock_onchain_metrics(price_df, seed=123):    """    Generate realistic mock on-chain metrics (MVRV, NUPL, SOPR)    correlated with BTC price. These follow historically-observed patterns:      - MVRV tends to rise in bull markets and fall in bear markets      - NUPL correlates with profit/loss sentiment      - SOPR oscillates around 1.0 (break-even line)    """    np.random.seed(seed)    n = len(price_df)    dates = price_df['date'].values    prices = price_df['price_usd'].values    # Calculate 365-day SMA for MVRV proxy    sma_365 = pd.Series(prices).rolling(365, min_periods=1).mean().values    # --- MVRV (Market Value to Realized Value Ratio) ---    # Historically ranges from ~0.5 (deep bear) to ~7+ (cycle tops)    # Correlate with price trend but add noise    price_ratio = prices / np.maximum(sma_365, 1)    mvrv = price_ratio * np.random.uniform(0.7, 1.3, n)    # Add cyclical component (simulate market cycles)    cycle_phase = np.linspace(0, 4 * np.pi, n)    mvrv += 0.3 * np.sin(cycle_phase)    mvrv = np.clip(mvrv, 0.4, 7.0)    # Smooth slightly    mvrv = pd.Series(mvrv).ewm(span=14).mean().values    # --- NUPL (Net Unrealized Profit/Loss) ---    # Ranges from -0.5 (deep capitulation) to 0.75+ (euphoria)    nupl_base = (prices - sma_365) / np.maximum(prices, 1)    nupl = nupl_base * 2.5 + np.random.normal(0, 0.08, n)    nupl = np.clip(nupl, -0.5, 0.8)    nupl = pd.Series(nupl).ewm(span=7).mean().values    # --- SOPR (Spent Output Profit Ratio) ---    # Oscillates around 1.0; < 1 means selling at loss, > 1 at profit    sopr_noise = np.random.normal(0, 0.05, n)    sopr_trend = 1.0 + 0.3 * (price_ratio - 1.0) + np.sin(cycle_phase * 0.7) * 0.1    sopr = sopr_trend + sopr_noise    sopr = np.clip(sopr, 0.5, 2.5)    sopr = pd.Series(sopr).ewm(span=7).mean().values    df = pd.DataFrame({        'date': dates,        'mvrv': mvrv,        'nupl': nupl,        'sopr': sopr    })    print(f"[DATA] Generated {n} days of mock on-chain metrics.")    print(f"        MVRV range: {mvrv.min():.2f} - {mvrv.max():.2f}")    print(f"        NUPL range: {nupl.min():.2f} - {nupl.max():.2f}")    print(f"        SOPR range: {sopr.min():.2f} - {sopr.max():.2f}")    return dfdef compute_technical_indicators(df):    """    Add technical indicators to the DataFrame:    - EMA 20 (20-day Exponential Moving Average)    - RSI 14  (14-day Relative Strength Index)    - SMA 365 (365-day Simple Moving Average) for MVRV proxy    """    prices = df['price_usd']    # EMA 20: A short-term trend indicator that reacts quickly to price changes    df['ema_20'] = prices.ewm(span=20, adjust=False).mean()    # RSI 14: Momentum oscillator (0-100). >70 = overbought, <30 = oversold    delta = prices.diff()    gain = delta.where(delta > 0, 0.0).ewm(span=14, adjust=False).mean()    loss = (-delta.where(delta < 0, 0.0)).ewm(span=14, adjust=False).mean()    rs = gain / loss.replace(0, np.nan)    df['rsi_14'] = 100 - (100 / (1 + rs))    df['rsi_14'] = df['rsi_14'].fillna(50)  # Default to neutral on insufficient data    # SMA 365: Long-term trend baseline for MVRV proxy fallback    df['sma_365'] = prices.rolling(365, min_periods=1).mean()    return dfdef build_master_dataframe(years=5):    """    Main data pipeline: fetch or mock data, merge into one master DataFrame    sorted by date with all required columns.    """    end_date = datetime.now().date()    start_date = end_date - timedelta(days=int(years * 365.25))    # --- Step 1: Get BTC Price Data ---    price_df = fetch_coingecko_btc_price(days=int(years * 365.25))    if price_df is None or len(price_df) < 365:        print("[DATA] Falling back to mock BTC prices...")        price_df = generate_mock_btc_prices(start_date, end_date)    # --- Step 2: Get On-Chain Metrics ---    # Try fetching from BGeometrics; fall back to mock if any fail    mvrv_df = fetch_bgeometrics_metric('mvrv')    nupl_df = fetch_bgeometrics_metric('nupl')    sopr_df = fetch_bgeometrics_metric('sopr')    need_mock = (mvrv_df is None or nupl_df is None or sopr_df is None)    if need_mock:        print("[DATA] Falling back to mock on-chain metrics...")        onchain_df = generate_mock_onchain_metrics(price_df)    else:        # Merge the three API DataFrames        onchain_df = price_df[['date']].copy()        for metric_df, col in [(mvrv_df, 'mvrv'), (nupl_df, 'nupl'), (sopr_df, 'sopr')]:            onchain_df = onchain_df.merge(metric_df, on='date', how='left')    # --- Step 3: Merge Price + On-Chain into Master DataFrame ---    master = price_df.merge(onchain_df, on='date', how='left')    master = master.sort_values('date').reset_index(drop=True)    # --- Step 4: Forward-fill missing on-chain values (handle gaps < 48h) ---    # On-chain data may have gaps; forward-fill up to 2 consecutive missing days    for col in ['mvrv', 'nupl', 'sopr']:        master[col] = master[col].ffill(limit=2)    # --- Step 5: Fallback — If still missing, use MVRV proxy = Price / 365-day SMA ---    # This ensures we always have a valuation signal even without on-chain data    master['mvrv_proxy'] = master['price_usd'] / master['sma_365'] if 'sma_365' not in master.columns:        master['sma_365'] = master['price_usd'].rolling(365, min_periods=1).mean()        master['mvrv_proxy'] = master['price_usd'] / master['sma_365']    master['mvrv'] = master['mvrv'].fillna(master['mvrv_proxy'])    # Fill NUPL and SOPR with neutral defaults if still missing    master['nupl'] = master['nupl'].fillna(0.0)    master['sopr'] = master['sopr'].fillna(1.0)    # --- Step 6: Add Technical Indicators ---    master = compute_technical_indicators(master)    # Add price in THB for convenience    master['price_thb'] = master['price_usd'] * USD_THB_RATE    print(f"\n[DATA] Master DataFrame ready: {len(master)} rows, "          f"{master['date'].min()} to {master['date'].max()}")    print(f"        Columns: {list(master.columns)}")    return master# ============================================================# SECTION 2: BACKTEST ENGINE — CORE LOGIC# ============================================================def apply_buy_fee(thb_amount):    """    Deduct 0.15% execution friction (fee + slippage) from a buy order.    Returns the effective THB that actually buys BTC.    """    return thb_amount * (1 - BUY_FEE_PCT)def apply_sell_fee(thb_amount):    """    Deduct 0.15% execution friction from a sell order.    Returns the net THB received after selling.    """    return thb_amount * (1 - SELL_FEE_PCT)def backtest_strategy(df, strategy_func, strategy_name):    """    Generic backtest runner. Takes a DataFrame and a strategy function,    returns a dict of results and a daily tracking DataFrame.        The strategy_func receives the current state dict and the current row,    and must return a dict with keys:        - buy_thb: amount of THB to spend on buying BTC (0 = no buy)        - sell_btc_pct: percentage of BTC holdings to sell (0 = no sell)        - to_reserve: THB amount to move to/from cash reserve        - notes: optional string description of the action    """    # State tracking variables    btc Holdings, cash reserve, total invested, etc.    btc = 0.0          # Total BTC accumulated    cash_reserve = 0.0 # Cash reserve pool (THB)    total_invested = 0.0 # Total THB spent out-of-pocket (including to reserve)    cooldown = 0        # Days until next sell is allowed    peak_value = 0.0   # For max drawdown calculation    max_drawdown = 0.0  # Worst drawdown observed    # Daily tracking for charting    daily_log = []    for idx, row in df.iterrows():        price_thb = row['price_thb']        price_usd = row['price_usd']        date = row['date']        # Decrement cooldown counter        if cooldown > 0:            cooldown -= 1        # --- Call the strategy function to get today's action ---        state = {            'btc': btc, 'cash_reserve': cash_reserve,            'total_invested': total_invested, 'cooldown': cooldown,            'row': row, 'idx': idx        }        action = strategy_func(state)        buy_thb = action.get('buy_thb', 0)        sell_btc_pct = action.get('sell_btc_pct', 0)        to_reserve = action.get('to_reserve', 0)        # --- Execute BUY ---        actual_buy = apply_buy_fee(buy_thb)        btc_bought = actual_buy / price_thb if price_thb > 0 else 0        btc += btc_bought        total_invested += buy_thb  # Track gross out-of-pocket        # --- Execute SELL ---        if sell_btc_pct > 0 and btc > 0:            btc_to_sell = btc * (sell_btc_pct / 100.0)            sell_proceeds = apply_sell_fee(btc_to_sell * price_thb)            btc -= btc_to_sell            cash_reserve += sell_proceeds  # Proceeds go to cash reserve            cooldown = action.get('new_cooldown', cooldown)        # --- Cash Reserve Management ---        cash_reserve += to_reserve        cash_reserve = max(cash_reserve, 0)  # Reserve can't go negative        # --- Portfolio Valuation ---        portfolio_value = btc * price_thb + cash_reserve        avg_cost = total_invested / btc if btc > 0 else 0        # --- Max Drawdown Calculation ---        if portfolio_value > peak_value:            peak_value = portfolio_value        if peak_value > 0:            drawdown = (peak_value - portfolio_value) / peak_value            if drawdown > max_drawdown:                max_drawdown = drawdown        # Log daily state        daily_log.append({            'date': date,            'price_thb': price_thb,            'btc': btc,            'cash_reserve': cash_reserve,            'total_invested': total_invested,            'portfolio_value': portfolio_value,            'avg_cost': avg_cost,            'max_drawdown_so_far': max_drawdown,            'buy_thb': buy_thb,            'btc_bought': btc_bought,        })    # --- Final Summary ---    final_price = df.iloc[-1]['price_thb']    final_value = btc * final_price + cash_reserve    final_avg_cost = total_invested / btc if btc > 0 else 0    roi_pct = ((final_value - total_invested) / total_invested * 100) if total_invested > 0 else 0    results = {        'strategy': strategy_name,        'total_invested': total_invested,        'total_btc': btc,        'avg_cost_thb': final_avg_cost,        'avg_cost_usd': final_avg_cost / USD_THB_RATE,        'final_value': final_value,        'cash_reserve': cash_reserve,        'roi_pct': roi_pct,        'max_drawdown_pct': max_drawdown * 100,        'final_price_thb': final_price,    }    daily_df = pd.DataFrame(daily_log)    return results, daily_df# ============================================================# SECTION 3: STRATEGY IMPLEMENTATIONS# ============================================================# --- STRATEGY 1: STANDARD DCA (BENCHMARK) ---def strategy_standard_dca(state):    """    The simplest strategy: buy 100 THB of BTC every single day,    unconditionally. No selling, no reserve, no on-chain analysis.    This is our benchmark to measure all other strategies against.    """    return {        'buy_thb': BASE_BUDGET_THB,        'sell_btc_pct': 0,        'to_reserve': 0,    }# --- STRATEGY 2: STYLE C (On-Chain Tiered Pure DCA) ---def strategy_style_c(state):    """    Style C: Pure accumulation (long-only, no selling, no cash reserve).    Scales daily buy amount based on MVRV tiers, with SOPR/NUPL boosters    for extra aggressiveness during extreme undervaluation.        MVRV Tiers:      < 1.0  → 3.0x base (300 THB) — deep value, buy heavy      1.0-1.5 → 2.0x base (200 THB) — moderately cheap      1.5-2.0 → 1.0x base (100 THB) — fair value      2.0-2.5 → 0.5x base (50 THB)  — getting expensive      >= 2.5  → 0.0x (0 THB) — pause, capital stays in bank    """    row = state['row']    mvrv = row['mvrv']    sopr = row['sopr']    nupl = row['nupl']    # Determine base multiplier from MVRV tier    if mvrv < 1.0:        multiplier = 3.0        # BOOSTER: If SOPR < 0.95 (selling at a loss → capitulation), buy even more        if sopr < 0.95:            multiplier = 4.5    elif mvrv < 1.5:        multiplier = 2.0        # BOOSTER: If NUPL < 0.25 (market largely at loss → fear), boost        if nupl < 0.25:            multiplier = 3.0    elif mvrv < 2.0:        multiplier = 1.0    elif mvrv < 2.5:        multiplier = 0.5    else:        multiplier = 0.0  # Pause buys — market is overheated    buy_amount = BASE_BUDGET_THB * multiplier    return {        'buy_thb': buy_amount,        'sell_btc_pct': 0,        'to_reserve': 0,    }# --- STRATEGY 3: STYLE E (Smart Rebalance & Top Skimming) ---def strategy_style_e(state):    """    Style E: Mean-reversion cyclic rebalancing with cash reserve management.    Key differences from Style C:    1. Has a Cash Reserve Pool that hoards cash during overheated periods    2. When MVRV >= 2.5, 100% of base budget goes INTO the reserve (not spent)    3. Single-stage Top Skimming: sells 12% of BTC when MVRV > 3.0    4. Reserve Injection: deploys reserve cash when MVRV < 1.2    """    row = state['row']    mvrv = row['mvrv']    cash = state['cash_reserve']    cooldown = state['cooldown']    buy_amount = 0.0    sell_pct = 0.0    to_reserve = 0.0    new_cooldown = cooldown    # --- BUYING: Same MVRV scale as Style C but NO SOPR/NUPL boosters ---    if mvrv < 1.0:        buy_amount = BASE_BUDGET_THB * 3.0    elif mvrv < 1.5:        buy_amount = BASE_BUDGET_THB * 2.0    elif mvrv < 2.0:        buy_amount = BASE_BUDGET_THB * 1.0    elif mvrv < 2.5:        buy_amount = BASE_BUDGET_THB * 0.5    else:        # MVRV >= 2.5: Route 100% of base budget to Cash Reserve        to_reserve = BASE_BUDGET_THB    # --- RESERVE INJECTION: Deploy reserve when market is cheap ---    if mvrv < 1.2 and cash > 0:        # Add 5% of cash reserve to daily buy budget, capped at +500 THB/day        injection = min(cash * 0.05, 500)        buy_amount += injection        to_reserve -= injection  # Reduce reserve by injected amount    # --- TOP SKIMMING: Sell when extremely overvalued ---    if mvrv > 3.0 and cooldown == 0 and state['btc'] > 0:        sell_pct = 12.0  # Sell 12% of total BTC holdings        new_cooldown = 30  # 30-day cooldown before next sell    return {        'buy_thb': buy_amount,        'sell_btc_pct': sell_pct,        'to_reserve': to_reserve,        'new_cooldown': new_cooldown,    }# --- STRATEGY 4: STYLE G v2 (Adaptive Hybrid Flagship) ---def strategy_style_g_v2(df_precomputed):    """    Style G v2: Dynamic framework using 730-day rolling percentiles to adapt    to macro regime shifts (e.g., ETF-era MVRV compression).        This is a factory function — it precomputes the rolling percentiles    and returns the actual strategy function. This avoids recalculating    the same rolling windows every single day.    """    # Precompute 730-day rolling percentiles for MVRV and NUPL    # These tell us where today's value sits historically (0 = all-time low, 100 = all-time high)    mvrv_pct = df_precomputed['mvrv'].rolling(730, min_periods=30).apply(        lambda x: pd.Series(x).rank(pct=True).iloc[-1] * 100, raw=False    ).values    nupl_pct = df_precomputed['nupl'].rolling(730, min_periods=30).apply(        lambda x: pd.Series(x).rank(pct=True).iloc[-1] * 100, raw=False    ).values    def strategy_func(state):        row = state['row']        idx = state['idx']        cash = state['cash_reserve']        cooldown = state['cooldown']        mvrv = row['mvrv']        nupl = row['nupl']        sopr = row['sopr']        rsi = row['rsi_14']        ema20 = row['ema_20']        price = row['price_usd']        # Get precomputed percentile values (default to 50 = neutral)        m_pct = mvrv_pct[idx] if not np.isnan(mvrv_pct[idx]) else 50        n_pct = nupl_pct[idx] if not np.isnan(nupl_pct[idx]) else 50        # --- SOPR Score (0-100): Lower SOPR = more fear = higher score (better buying) ---        if sopr < 0.95:            sopr_score = 100        elif sopr < 0.98:            sopr_score = 75        elif sopr < 1.00:            sopr_score = 50        else:            sopr_score = 20        # --- Composite Value Score (S_t): 0-100, higher = more undervalued ---        # Weights: 45% MVRV percentile (inverted), 35% NUPL percentile (inverted), 20% SOPR score        # Inverted because LOW percentile = historically cheap = good time to buy        s_t = (0.45 * (100 - m_pct) +                  0.35 * (100 - n_pct) +                  0.20 * sopr_score)        # --- Out-of-Pocket Multiplier based on S_t ---        if s_t < 20:            oop_mult = 0.0   # Extreme overvaluation — pause buying        elif s_t < 40:            oop_mult = 0.5   # Overvalued — minimal buying        elif s_t < 70:            oop_mult = 1.0   # Neutral — standard DCA        elif s_t < 85:            oop_mult = 2.0   # Undervalued — increase buying        elif s_t < 92:            oop_mult = 3.5   # Very undervalued — aggressive buying        else:            oop_mult = 5.0   # Extremely undervalued — maximum buying        buy_amount = BASE_BUDGET_THB * oop_mult        # Unspent base budget goes to Cash Reserve        # If multiplier < 1.0, the remainder of the 100 THB base goes to reserve        if oop_mult < 1.0:            to_reserve = BASE_BUDGET_THB * (1.0 - oop_mult)        else:            to_reserve = 0.0        # --- Drip Injection: Deploy cash reserve in cheap conditions ---        injection = 0.0        if s_t >= 80 and cash > 0:            if s_t >= 92 or sopr < 0.95:                injection = cash * 0.10  # 10% of reserve            elif s_t >= 88:                injection = cash * 0.08  # 8% of reserve            else:                injection = cash * 0.06  # 6% of reserve            injection = min(injection, 600)  # Cap at 600 THB/day            buy_amount += injection            to_reserve -= injection        # --- Two-Stage Top Skim (Sell 12% of BTC if cooldown == 0) ---        sell_pct = 0.0        new_cooldown = cooldown        if cooldown == 0 and state['btc'] > 0:            # Stage 1 (Soft Skim): MVRV is at or above 90th percentile            if m_pct >= 90:                sell_pct = 12.0                new_cooldown = 45            # Stage 2 (Trend Break): Score crashed AND price below EMA20 AND not overbought            elif s_t < 15 and price < ema20 and rsi < 68:                sell_pct = 12.0                new_cooldown = 45        return {            'buy_thb': buy_amount,            'sell_btc_pct': sell_pct,            'to_reserve': to_reserve,            'new_cooldown': new_cooldown,        }    return strategy_func# --- STRATEGY 5: STYLE ALPHA (Innovated — Designed to Outperform) ---def strategy_style_alpha(df_precomputed):    """    ╔══════════════════════════════════════════════════════════════════╗    ║  SMART DCA STYLE ALPHA — Adaptive Volatility-Regime Strategy   ║    ╚══════════════════════════════════════════════════════════════════╝    DESIGN PHILOSOPHY:    Style Alpha addresses the structural weaknesses identified in Styles C, E, and G v2:      WEAKNESS 1 (Style C): No profit harvesting means the portfolio never locks in    gains during euphoric tops. All accumulated BTC is exposed to full drawdowns.    → FIX: Alpha uses a multi-stage fractional scale-out system that sells in    gradual tranches (5% → 10% → 15%) instead of dumping 12% at once.      WEAKNESS 2 (Style E): Sells too early in extended bull runs because the MVRV > 3.0    trigger can fire mid-bull when MVRV compresses post-ETF. Also, the 30-day cooldown    is too short, causing repeated premature selling.    → FIX: Alpha uses a 200-day SMA regime filter. In a confirmed bull regime    (price > 200-day SMA), the sell threshold is raised significantly, preventing    premature profit-taking during sustained uptrends.    → FIX: Alpha uses an adaptive cooldown (60-90 days) that scales with    volatility to avoid whipsaw selling.      WEAKNESS 3 (Style G v2): Cash drag — too much capital sits idle in the reserve    pool during long bear markets. The 730-day rolling window can be slow to adapt    to rapid regime changes. The drip injection is too conservative.    → FIX: Alpha uses a shorter 365-day lookback for faster regime detection,    combined with a Risk Parity-style reserve allocation that maintains a target    reserve-to-portfolio ratio (20%) rather than letting it grow unbounded.    → FIX: Alpha's reserve injection is more aggressive during extreme fear:    up to 15% of reserve per day with a higher cap.    ADDITIONAL INNOVATIONS:    1. Volatility-Adjusted Sizing: Daily buy amounts are scaled by inverse volatility.       During low-vol periods (fear/complacency), we buy more per unit of risk.       During high-vol periods (crashes/mania), position sizes are modulated to       avoid buying into a falling knife or chasing a parabolic move.    2. Regime Filter (200-day SMA): Determines if we're in a macro bull or bear.       This fundamentally changes the sell thresholds and reserve deployment rate.    3. Fractional Scale-Out: Instead of selling 12% all at once, Alpha sells    in escalating tranches based on overvaluation severity. This averages out    the sell price and reduces regret from selling too early or too late.    ================================================================    MATHEMATICAL FORMULAS:    ================================================================    1. REGIME DETECTION:       Regime = BULL if Price > 200-day SMA, else BEAR    2. REALIZED VOLATILITY (30-day):       Vol_30 = std(daily_returns, 30) * sqrt(365)       Vol_Score = clamp(Vol_30 / 0.80, 0.5, 2.0)  # Normalized to baseline 80% annual vol    3. VALUATION SCORE (V_t, 0-100):       V_t = 0.40*(100 - MVRV_pct365) + 0.30*(100 - NUPL_pct365) + 0.15*SOPR_score + 0.15*Regime_score       where: Regime_score = 80 if BEAR else 20    4. BUY MULTIPLIER (volatility-adjusted):       base_mult = f(V_t): <15→0, <30→0.5, <55→1.0, <75→2.0, <90→3.5, ≥90→5.0       effective_mult = base_mult / Vol_Score  (buy LESS when vol is extreme)       buy_amount = BASE_BUDGET * effective_mult    5. RESERVE MANAGEMENT (Risk Parity target 20%):       target_reserve = portfolio_value * 0.20       If reserve > target_reserve * 1.5: surplus → inject to buys       If reserve < target_reserve * 0.5 and V_t > 60: boost reserve funding    6. FRACTIONAL SCALE-OUT (sell BTC when overvalued, cooldown == 0):       In BULL regime (higher threshold to avoid premature selling):         V_t < 10 AND RSI > 75 → sell 5%         V_t < 5  AND RSI > 80 → sell 10%       In BEAR regime (lower threshold, defensive):         MVRV_pct > 92 → sell 5%         MVRV_pct > 97 → sell 10%         MVRV > 3.5 → sell 15%       Cooldown = 60 + int(Vol_Score * 15) days (60-90 days, volatility-adaptive)    """    # --- PRECOMPUTATION: Calculate rolling indicators once ---    # Use 365-day rolling percentiles for faster regime detection than G v2's 730-day    mvrv_pct = df_precomputed['mvrv'].rolling(365, min_periods=30).apply(        lambda x: pd.Series(x).rank(pct=True).iloc[-1] * 100, raw=False    ).values    nupl_pct = df_precomputed['nupl'].rolling(365, min_periods=30).apply(        lambda x: pd.Series(x).rank(pct=True).iloc[-1] * 100, raw=False    ).values    # 30-day annualized realized volatility    returns = df_precomputed['price_usd'].pct_change()    vol_30 = returns.rolling(30, min_periods=5).std() * np.sqrt(365)    vol_30 = vol_30.fillna(0.60).values  # Default to 60% if insufficient data    # 200-day SMA for regime detection    sma_200 = df_precomputed['price_usd'].rolling(200, min_periods=50).mean().values    def strategy_func(state):        row = state['row']        idx = state['idx']        cash = state['cash_reserve']        cooldown = state['cooldown']        btc = state['btc']        price_usd = row['price_usd']        price_thb = row['price_thb']        mvrv = row['mvrv']        nupl = row['nupl']        sopr = row['sopr']        rsi = row['rsi_14']        total_inv = state['total_invested']        # --- 1. REGIME DETECTION ---        # Bull = price above 200-day SMA; Bear = below        sma200 = sma_200[idx] if idx < len(sma_200) else price_usd        is_bull = price_usd > sma200 if not np.isnan(sma200) else True        regime_score = 20 if is_bull else 80  # Bear = high score (cheap = buy more)        # --- 2. VOLATILITY SCORING ---        vol = vol_30[idx] if idx < len(vol_30) else 0.60        # Normalize: clamp between 0.5x and 2.0x with 80% annual vol as baseline        vol_score = np.clip(vol / 0.80, 0.5, 2.0)        # --- 3. SOPR SCORE (same as G v2) ---        if sopr < 0.95:            sopr_score = 100        elif sopr < 0.98:            sopr_score = 75        elif sopr < 1.00:            sopr_score = 50        else:            sopr_score = 20        # --- 4. VALUATION SCORE (V_t, 0-100) ---        # Higher V_t = more undervalued = better time to buy aggressively        m_pct = mvrv_pct[idx] if not np.isnan(mvrv_pct[idx]) else 50        n_pct = nupl_pct[idx] if not np.isnan(nupl_pct[idx]) else 50        v_t = (0.40 * (100 - m_pct) +                0.30 * (100 - n_pct) +                0.15 * sopr_score +                0.15 * regime_score)        # --- 5. BUY MULTIPLIER (base, before volatility adjustment) ---        if v_t < 15:            base_mult = 0.0       # Extreme overvaluation        elif v_t < 30:            base_mult = 0.5       # Overvalued        elif v_t < 55:            base_mult = 1.0       # Neutral        elif v_t < 75:            base_mult = 2.0       # Undervalued        elif v_t < 90:            base_mult = 3.5       # Very undervalued        else:            base_mult = 5.0       # Extremely undervalued        # Volatility adjustment: divide by vol_score        # This means: in HIGH vol (crashes/parabolas), we buy LESS (size down)        # In LOW vol (quiet accumulation), we buy MORE per unit        effective_mult = base_mult / vol_score        buy_amount = BASE_BUDGET_THB * effective_mult        # --- 6. RESERVE MANAGEMENT (Risk Parity with 20% target) ---        to_reserve = 0.0        portfolio_val = btc * price_thb + cash        if portfolio_val > 0:            target_reserve = portfolio_val * 0.20            # If reserve is too large (>150% of target), inject surplus into buys            if cash > target_reserve * 1.5 and v_t > 40:                surplus = cash - target_reserve * 1.5                injection = min(surplus * 0.15, 800)  # Inject 15% of surplus, cap at 800 THB                buy_amount += injection                to_reserve -= injection            # If reserve is too small and we're in a good buying zone, fund it            elif cash < target_reserve * 0.5 and v_t < 30:                # Divert some budget to build reserve                reserve_fund = BASE_BUDGET_THB * 0.3                to_reserve = reserve_fund                buy_amount = max(0, buy_amount - reserve_fund)        # Ensure buy_amount is not negative after reserve funding        buy_amount = max(0, buy_amount)        # --- 7. FRACTIONAL SCALE-OUT (Progressive Selling) ---        sell_pct = 0.0        new_cooldown = cooldown        if cooldown == 0 and btc > 0:            if is_bull:                # BULL REGIME: Very high sell thresholds (avoid premature selling)                if v_t < 10 and rsi > 75:                    sell_pct = 5.0    # Mild trimming                elif v_t < 5 and rsi > 80:                    sell_pct = 10.0  # Strong trimming            else:                # BEAR REGIME: Lower thresholds (defensive, take profits while we can)                if m_pct > 92:                    sell_pct = 5.0                elif m_pct > 97:                    sell_pct = 10.0                elif mvrv > 3.5:                    sell_pct = 15.0  # Extreme overvaluation, sell aggressively            if sell_pct > 0:                # Adaptive cooldown: 60-90 days, scales with volatility                new_cooldown = 60 + int(vol_score * 15)        return {            'buy_thb': buy_amount,            'sell_btc_pct': sell_pct,            'to_reserve': to_reserve,            'new_cooldown': new_cooldown,        }    return strategy_func# ============================================================# SECTION 4: SUMMARY & VISUALIZATION# ============================================================def print_summary_table(all_results):    """    Print a formatted comparison table of all strategy results.    """    print("\n" + "=" * 100)    print("  BACKTEST RESULTS — STRATEGY COMPARISON")    print("=" * 100)    # Header    header = (f"{'Strategy':<18} {'Invested (THB)':>16} {'BTC Acc.':>12} "                f"{'Avg Cost/BTC':>14} {'Final Value':>16} {'ROI %':>10} {'Max DD %':>10}")    print(header)    print("-" * 100)    for r in all_results:        line = (f"{r['strategy']:<18} {r['total_invested']:>16,.0f} {r['total_btc']:>12.6f} "                f"{r['avg_cost_thb']:>14,.0f} {r['final_value']:>16,.0f} "                f"{r['roi_pct']:>9.1f}% {r['max_drawdown_pct']:>9.1f}%")        print(line)    print("=" * 100)    # Find the best performer    best = max(all_results, key=lambda x: x['final_value'])    print(f"\n  Best Final Value: {best['strategy']} ({best['final_value']:,.0f} THB)")    best_roi = max(all_results, key=lambda x: x['roi_pct'])    print(f"  Best ROI: {best_roi['strategy']} ({best_roi['roi_pct']:.1f}%)")    lowest_cost = min(all_results, key=lambda x: x['avg_cost_thb'] if x['avg_cost_thb'] > 0 else float('inf'))    print(f"  Lowest Avg Cost: {lowest_cost['strategy']} ({lowest_cost['avg_cost_thb']:,.0f} THB/BTC)")    lowest_dd = min(all_results, key=lambda x: x['max_drawdown_pct'])    print(f"  Lowest Max Drawdown: {lowest_dd['strategy']} ({lowest_dd['max_drawdown_pct']:.1f}%)")    print()def generate_charts(all_daily_dfs, all_results, years_label):    """    Generate two comparison charts:    1. Portfolio Value over time for all strategies    2. Average Cost per BTC over time for all strategies    """    colors = ['#9E9E9E', '#2196F3', '#FF9800', '#9C27B0', '#E91E63']    styles_names = [r['strategy'] for r in all_results]    fig, axes = plt.subplots(2, 1, figsize=(16, 12), constrained_layout=True)    fig.suptitle(f'Smart DCA Strategy Comparison ({years_label})', fontsize=18, fontweight='bold', y=1.02)    # --- CHART 1: Portfolio Value Over Time ---    ax1 = axes[0]    for i, (name, daily_df) in enumerate(zip(styles_names, all_daily_dfs)):        ax1.plot(daily_df['date'], daily_df['portfolio_value'],                 label=name, color=colors[i], linewidth=1.5, alpha=0.9)    ax1.set_title('Portfolio Value (THB) Over Time', fontsize=14, fontweight='bold')    ax1.set_xlabel('Date', fontsize=11)    ax1.set_ylabel('Portfolio Value (THB)', fontsize=11)    ax1.yaxis.set_major_formatter(mticker.FuncFormatter(lambda x, p: f'{x:,.0f}'))    ax1.legend(loc='upper left', fontsize=10, framealpha=0.9)    ax1.grid(True, alpha=0.3, linestyle='--')    ax1.tick_params(axis='x', rotation=30)    # --- CHART 2: Average Cost per BTC Over Time ---    ax2 = axes[1]    for i, (name, daily_df) in enumerate(zip(styles_names, all_daily_dfs)):        # Only plot where avg_cost > 0 (i.e., we've bought some BTC)        valid = daily_df[daily_df['avg_cost'] > 0]        ax2.plot(valid['date'], valid['avg_cost'],                 label=name, color=colors[i], linewidth=1.5, alpha=0.9)    ax2.set_title('Average Cost per BTC (THB) Over Time', fontsize=14, fontweight='bold')    ax2.set_xlabel('Date', fontsize=11)    ax2.set_ylabel('Avg Cost / BTC (THB)', fontsize=11)    ax2.yaxis.set_major_formatter(mticker.FuncFormatter(lambda x, p: f'{x:,.0f}'))    ax2.legend(loc='upper right', fontsize=10, framealpha=0.9)    ax2.grid(True, alpha=0.3, linestyle='--')    ax2.tick_params(axis='x', rotation=30)    plt.savefig(os.path.join(DOWNLOAD_DIR, f'smart_dca_comparison_{years_label}.png'), dpi=150, bbox_inches='tight')    print(f"[CHART] Saved: {DOWNLOAD_DIR}/smart_dca_comparison_{years_label}.png")    plt.close()# ============================================================# SECTION 5: MAIN EXECUTION# ============================================================def run_backtest(df, strategy_func, strategy_name):    """    Wrapper that handles the precomputed-data pattern for G v2 and Alpha.    """    if callable(strategy_func) and not hasattr(strategy_func, '__name__'):        # It's already a function returned from a factory        func = strategy_func    else:        func = strategy_func    return backtest_strategy(df, func, strategy_name)def main():    """    Main entry point: build data, run all strategies, print results, save charts.    """    print("\n[PHASE 1] Building data pipeline...")    # Build master DataFrame with 5 years of data    master_df = build_master_dataframe(years=5)    # Define strategies to test    strategies = [        ('Standard DCA', strategy_standard_dca),        ('Style C', strategy_style_c),        ('Style E', strategy_style_e),    ]    # Precompute-dependent strategies (factory pattern)    strategies.append(('Style G v2', strategy_style_g_v2(master_df)))    strategies.append(('Style Alpha', strategy_style_alpha(master_df)))    # Run backtests for BOTH 3-year and 5-year periods    for years in [3, 5]:        label = f'{years}-Year'        print(f"\n{'=' * 70}")        print(f"  RUNNING {label.upper()} BACKTEST")        print(f"{'=' * 70}")        # Slice DataFrame for the period        if years == 3:            test_df = master_df.tail(int(3 * 365.25)).reset_index(drop=True)        else:            test_df = master_df.copy()        print(f"  Period: {test_df['date'].iloc[0]} to {test_df['date'].iloc[-1]} ({len(test_df)} days)")        # Recreate factory-based strategies for the sliced DataFrame        period_strategies = [            ('Standard DCA', strategy_standard_dca),            ('Style C', strategy_style_c),            ('Style E', strategy_style_e),            ('Style G v2', strategy_style_g_v2(test_df)),            ('Style Alpha', strategy_style_alpha(test_df)),        ]        all_results = []        all_daily_dfs = []        for name, func in period_strategies:            print(f"\n  Backtesting {name}...")            results, daily_df = backtest_strategy(test_df, func, name)            all_results.append(results)            all_daily_dfs.append(daily_df)            print(f"    Invested: {results['total_invested']:,.0f} THB | "                  f"BTC: {results['total_btc']:.6f} | "                  f"Avg Cost: {results['avg_cost_thb']:,.0f} THB | "                  f"Value: {results['final_value']:,.0f} THB | "                  f"ROI: {results['roi_pct']:.1f}% | "                  f"Max DD: {results['max_drawdown_pct']:.1f}%")        # Print summary table        print_summary_table(all_results)        # Generate charts        generate_charts(all_daily_dfs, all_results, label.replace(' ', '_'))    # ============================================================    # PHASE 2: RESEARCH ANALYSIS (printed as output)    # ============================================================    print("\n" + "#" * 100)    print("#  PHASE 2: RESEARCH ANALYSIS & STYLE ALPHA DESIGN RATIONALE")    print("#" * 100)    print("""  STRUCTURAL WEAKNESSES IDENTIFIED IN EXISTING STRATEGIES:  ─────────────────────────────────────────────────────    STYLE C (On-Chain Tiered Pure DCA):    • NO PROFIT HARVESTING: Being long-only with no selling mechanism means the    portfolio is fully exposed to every drawdown. In a cycle where BTC goes from    $69K → $15K (2022), the entire accumulated position loses ~78% of its value    with no mechanism to lock in any gains from the $69K peak.    • NO CASH RESERVE: Cannot capitalize on extreme buying opportunities because    there's no stored capital to deploy during capitulation events.    • STATIC THRESHOLDS: The MVRV tiers are fixed (1.0, 1.5, 2.0, 2.5). In a    post-ETF regime where MVRV may structurally compress, these thresholds    may never trigger 